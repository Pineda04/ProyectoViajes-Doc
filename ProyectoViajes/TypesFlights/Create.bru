meta {
  name: Create
  type: http
  seq: 3
}

post {
  url: {{PATH}}/types_flight
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "name": "string2",
    "description": "string"
  }
}
